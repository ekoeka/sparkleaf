import 'package:flutter/material.dart';

class SignBackgroundColor extends StatelessWidget {
  const SignBackgroundColor({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Color(0xFF01D150));
  }
}
